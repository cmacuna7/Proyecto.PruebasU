# Puerto del servidor (Render usa PORT=10000 por defecto)
PORT=3000

# Clave secreta para JWT (cambia esto en producción)
JWT_SECRET=cambia_esta_clave_secreta

# Base de datos MongoDB Atlas (por defecto)
MONGO_URL=mongodb+srv://FCaetano:FCaetano@cluster0.erktrrv.mongodb.net/concesionaria?retryWrites=true&w=majority&appName=Cluster0
# Para desarrollo local: mongodb://localhost:27017/concesionaria  
# Para Railway usará DATABASE_URL automáticamente cuando tengas MongoDB configurado

# Configuración para Render
NODE_ENV=production
