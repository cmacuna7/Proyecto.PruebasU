================================================================================
RESUMEN DE PRUEBAS ANGULAR - SERVICIO DE VENDEDORES (vendor.service.spec.ts)
================================================================================

TOTAL: 12 PRUEBAS

================================================================================
1. PRUEBAS DE OBTENCIÓN DE DATOS
================================================================================

TEST: "debería devolver lista vacía inicialmente"
- Valida que la lista de vendedores comience vacía
- Verifica que sea un Array válido
- Resultado esperado: vendedores.length === 0 y Array.isArray() === true

================================================================================
2. PRUEBAS DE CREACIÓN (POST)
================================================================================

TEST: "debería crear un nuevo vendedor"
- Valida que se puede agregar un vendedor a la lista
- Datos: Ana García, ana.garcia@concesionaria.com, 0987654321, comisión 20, EMP-123
- Verifica: lista tiene 1 elemento, nombre coincide, código empleado correcto
- Resultado esperado: vendedor creado con ID y datos intactos

TEST: "debería fallar si faltan campos obligatorios"
- Valida que no se puede crear vendedor incompleto
- Caso de prueba: solo incluye nombre, faltan email, teléfono, comisión y código
- Verifica presencia de todas las propiedades requeridas
- Resultado esperado: false (no es válido)

================================================================================
3. PRUEBAS DE ACTUALIZACIÓN (PUT)
================================================================================

TEST: "debería actualizar un vendedor existente"
- Valida que se pueden modificar datos de un vendedor
- Cambios: comisión de 20 a 25, teléfono actualizado
- Verifica ambos campos actualizados correctamente
- Resultado esperado: vendedor modificado mantiene ID pero con nuevos datos

================================================================================
4. PRUEBAS DE ELIMINACIÓN (DELETE)
================================================================================

TEST: "debería eliminar un vendedor"
- Valida que se puede eliminar un vendedor de la lista
- Procedimiento: agregar vendedor (length=1) → eliminar → verificar (length=0)
- Resultado esperado: lista vacía después de eliminar

================================================================================
5. PRUEBAS DE VALIDACIÓN - COMISIÓN
================================================================================

TEST: "debería rechazar comisión mayor a 100"
- Valida que comisión debe estar entre 0 y 100
- Caso de prueba: comisión = 150
- Verificación: comision >= 0 && comision <= 100
- Resultado esperado: false (no es válido)

TEST: "debería rechazar comisión negativa"
- Valida que comisión no puede ser negativa
- Caso de prueba: comisión = -10
- Verificación: comision >= 0 && comision <= 100
- Resultado esperado: false (no es válido)

================================================================================
6. PRUEBAS DE VALIDACIÓN - UNICIDAD
================================================================================

TEST: "debería rechazar email duplicado"
- Valida que no pueden existir dos vendedores con mismo email
- Procedimiento: agrega vendedor1 con email "ana.garcia@concesionaria.com"
- Intenta agregar vendedor2 con mismo email
- Verificación: busca si email ya existe en lista
- Resultado esperado: true (email encontrado, falla validación)

================================================================================
7. PRUEBAS DE VALIDACIÓN - CAMPOS VACÍOS
================================================================================

TEST: "debería rechazar nombre vacío"
- Valida que nombre es obligatorio y no vacío
- Caso de prueba: name = ""
- Verificación: !!(name && name.trim().length > 0)
- Resultado esperado: false (no es válido)

TEST: "debería rechazar email vacío"
- Valida que email es obligatorio y no vacío
- Caso de prueba: email = ""
- Verificación: !!(email && email.trim().length > 0)
- Resultado esperado: false (no es válido)

TEST: "debería rechazar teléfono vacío"
- Valida que teléfono es obligatorio y no vacío
- Caso de prueba: telefono = ""
- Verificación: !!(telefono && telefono.trim().length > 0)
- Resultado esperado: false (no es válido)

TEST: "debería rechazar código de empleado vacío"
- Valida que código de empleado es obligatorio y no vacío
- Caso de prueba: codigoEmpleado = ""
- Verificación: !!(codigoEmpleado && codigoEmpleado.trim().length > 0)
- Resultado esperado: false (no es válido)

================================================================================
COBERTURA DE PRUEBAS
================================================================================

OPERACIONES BÁSICAS (4 tests):
✓ Obtener lista vacía
✓ Crear vendedor (POST)
✓ Actualizar vendedor (PUT)
✓ Eliminar vendedor (DELETE)

VALIDACIONES DE CAMPOS (4 tests):
✓ Nombre no vacío
✓ Email no vacío
✓ Teléfono no vacío
✓ Código de empleado no vacío

VALIDACIONES DE NEGOCIO (4 tests):
✓ Campos obligatorios presentes
✓ Comisión entre 0-100
✓ Comisión negativa rechazada
✓ Email duplicado rechazado

================================================================================
RESULTADO ESPERADO
================================================================================

TOTAL: 12 PRUEBAS
✓ 12 PASSING
✗ 0 FAILING

COBERTURA:
- Statements: 100%
- Branches: 100%
- Functions: 100%
- Lines: 100%

================================================================================
ARCHIVO DE PRUEBA
================================================================================

Ubicación: lab1.angular/src/app/vendor.service.spec.ts

Formato: Jasmine/Karma
Tipo: Unit Tests
Framework: Angular Testing
